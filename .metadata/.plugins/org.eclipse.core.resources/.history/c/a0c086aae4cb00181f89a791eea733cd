package com.qa.ExamResults;

public class Logic {
	static individualSubject individualPercentage = a -> (a * 100 / 450);
	static overallSubject overallPercentage = (a, b, c) -> (((a + b + c) * 100 / 450));

	public static double result(double a, individualSubject isubject) {
		return isubject.result(a);
	}

	public static double overallResult(double a, double b, double c, overallSubject osubject) {
		return osubject.overallResult(a, b, c);
	}

	interface individualSubject {
		double result(double a);

	}

	interface overallSubject {
		double overallResult(double a, double b, double c);
	}

}
